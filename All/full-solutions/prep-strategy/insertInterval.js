function insertInterval(intervals,newInterval){

   //steo 1: create a result array
    //step 2: iterate through the intervals
    //step 3: if the current interval is less than the new interval, push the current interval to the result array
    //step 4: if the current interval is greater than the new interval, push the new interval to the result array
    //step 5: if the current interval overlaps with the new interval, update the new interval to the merged interval
    //step 6: return the result array

    let result = []
    let i = 0



}